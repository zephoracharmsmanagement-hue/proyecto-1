# Secretos — NUNCA subir a git.
# El token de Meta vive aquí y solo aquí.
.env
.env.*
!.env.example

# Reportes generados por los scripts de meta/
meta/salida/

node_modules/
.DS_Store

# Dependencias de las pruebas
pruebas/node_modules/
pruebas/capturas/

# Comparaciones generadas por herramientas/entrar_fotos.py
herramientas/salida/

# Carpeta de subida de fotos: material en bruto, no se publica.
assets/webp2/

__pycache__/

# Material en bruto y capturas con datos de clientas: NO se publica.
material-sin-publicar/

# Espacio de trabajo de subagent-driven-development: el registro (ledger) y
# los paquetes de revisión de una sesión, no código. Si algún día se comitea
# por error, con publish = "." quedaría servido en el sitio — que no lo esté
# hoy es porque nunca se comiteó, no porque este archivo lo impidiera.
.superpowers/
